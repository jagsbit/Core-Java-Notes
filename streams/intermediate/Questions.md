# Java Streams — Intermediate Practice Questions

---

## 1. Count Elements in a List
Count the total number of elements in a list of integers.

**Input:**
```
[9, 2, 3, 4, 8, 16, 7]
```
**Output:**
```
7
```

---

## 2. Count Occurrence of Each Word in a List
Given a list of strings, count how many times each word appears.

**Input:**
```
["apple", "banana", "apple", "orange"]
```
**Output:**
```
{apple=2, banana=1, orange=1}
```

---

## 3. Count Occurrence of Each Word in a Sentence
Given a sentence, count how many times each word appears.

**Input:**
```
"I am a developer and a tester"
```
**Output:**
```
{I=1, am=1, a=2, developer=1, and=1, tester=1}
```

---

## 4. Factorial of a Number Using Streams
Calculate the factorial of a given number using Java Streams.

**Input:**
```
5
```
**Output:**
```
120
```

---

## 5. Find the Difference of Two Lists
Find elements that are present in the first list but **not** in the second list.

**Input:**
```
L1 = [1, 2, 3, 4, 5, 6]
L2 = [2, 4, 1, 3, 6, 8, 9]
```
**Output:**
```
[5]
```

---

## 6. Find the Intersection of Two Lists
Find elements that are **common** to both lists.

**Input:**
```
L1 = [1, 2, 3, 4, 5, 6]
L2 = [2, 4, 1, 3, 6, 8, 9]
```
**Output:**
```
[1, 2, 3, 4, 6]
```

---

## 7. Find the Union of Two Lists
Find all **distinct** elements from both lists combined.

**Input:**
```
L1 = [1, 2, 3, 4, 5, 6]
L2 = [2, 4, 1, 3, 6, 8, 9]
```
**Output:**
```
[1, 2, 3, 4, 5, 6, 8, 9]
```

---

## 8. Group Objects by a Field
Given a list of `Person` objects, group them by their age.

**Input:**
```
[Alice(age=25), Bob(age=30), Charlie(age=25)]
```
**Output:**
```
{25=[Alice : 25, Charlie : 25], 30=[Bob : 30]}
```

---

## 9. Group Strings by Length
Given a list of strings, group them by their character length.

**Input:**
```
["Ram", "Hari", "ansh", "sky"]
```
**Output:**
```
{3=[Ram, sky], 4=[Hari, ansh]}
```

---

## 10. Find the Longest Word in a Sentence
Given a sentence, find the longest word in it.

**Input:**
```
"I am learning streams API in Java"
```
**Output:**
```
learning
```

---

## 11. Find the Longest String in a List
Given a list of strings, find the string with the maximum length.

**Input:**
```
["hamza", "Anshuman", "Vaibav", "Venu"]
```
**Output:**
```
Anshuman
```

---

## 12. Merge Two Lists into One
Combine two separate lists into a single list while preserving the order.

**Input:**
```
List1 = [1, 2, 3]
List2 = [4, 5, 6]
```
**Output:**
```
[1, 2, 3, 4, 5, 6]
```

---

## 13. Count Occurrence of Each Character in a String
Given a string, count how many times each character appears.

**Input:**
```
"abcdabcd"
```
**Output:**
```
{a=2, b=2, c=2, d=2}
```

---

## 14. Count Occurrence of Each Digit in a String
Given a string containing letters and digits, count how many times each **digit** appears.

**Input:**
```
"hello 123 i am the 10"
```
**Output:**
```
{0=1, 1=2, 2=1, 3=1}
```

---

## 15. Count Occurrence of Each Vowel in a String
Given a string, count how many times each **vowel** (a, e, i, o, u) appears.

**Input:**
```
"hello world"
```
**Output:**
```
{e=1, o=2}
```

---

## 16. Partition a List into Even and Odd Numbers
Given a list of integers, separate them into two groups: even numbers and odd numbers.

**Input:**
```
[1, 2, 3, 4, 5, 6, 8]
```
**Output:**
```
{false=[1, 3, 5], true=[2, 4, 6, 8]}
```

---

## 17. Remove Duplicates from a List
Given a list of integers with duplicate values, remove all duplicates and return unique elements.

**Input:**
```
[1, 1, 2, 2, 3, 3, 4, 5, 5, 6]
```
**Output:**
```
[1, 2, 3, 4, 5, 6]
```

---

## 18. Remove Duplicate Characters from a String
Given a string with repeated characters, remove duplicates and keep only the first occurrence of each character.

**Input:**
```
"dabcdabc"
```
**Output:**
```
"dabc"
```

---

## 19. Reverse a List
Given a list of integers, reverse the order of its elements.

**Input:**
```
[1, 2, 3, 4, 5, 6]
```
**Output:**
```
[6, 5, 4, 3, 2, 1]
```

---

## 20. Reverse a String Using Streams
Given a string, reverse it using Java Streams.

**Input:**
```
"abcd"
```
**Output:**
```
"dcba"
```

---

## 21. Find the Second Largest Element
Given a list of integers, find the second largest element.

**Input:**
```
[1, 2, 3, 4, 5, 6, 7]
```
**Output:**
```
6
```

---

## 22. Sort Strings by Length
Given a list of strings, sort them in ascending order of their length.

**Input:**
```
["Nitish", "Anshu", "Jagannath", "Hamza"]
```
**Output:**
```
[Anshu, Hamza, Nitish, Jagannath]
```

---

## 23. Sort a List in Descending Order
Given a list of integers, sort them in descending order.

**Input:**
```
[9, 2, 3, 4, 8, 16, 7]
```
**Output:**
```
[16, 9, 8, 7, 4, 3, 2]
```

---

## 24. Sum of Digits of a Number
Given a number, find the sum of all its individual digits.

**Input:**
```
123456
```
**Output:**
```
21
```

---
